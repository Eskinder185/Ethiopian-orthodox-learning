import ContentCard from '../cards/ContentCard.jsx'

/**
 * Semantic alias for hub / topic cards — same visuals as ContentCard.
 */
export default function SectionCard(props) {
  return <ContentCard {...props} />
}
