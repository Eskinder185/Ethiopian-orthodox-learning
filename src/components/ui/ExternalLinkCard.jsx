import ExternalResourceCard from '../sections/ExternalResourceCard.jsx'

/**
 * Semantic alias for outbound resource cards (opens on original site).
 */
export default function ExternalLinkCard(props) {
  return <ExternalResourceCard {...props} />
}
