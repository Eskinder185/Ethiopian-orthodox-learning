import { paths } from '../constants/paths.js'

/**
 * Practice hub topic cards: `key` maps to `practiceHub.topics.<key>` in i18n.
 */
export const practiceHubTopicConfig = [
  { to: paths.practice.prayer, key: 'prayer' },
  { to: paths.practice.chants, key: 'chants' },
  { to: paths.practice.instruments, key: 'instruments' },
  { to: paths.practice.language.index, key: 'language' },
]
